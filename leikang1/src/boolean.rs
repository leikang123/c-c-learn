
fn main(){
    // 定义x为true
    let x=true;
    // 定义y为false
    let y=false;
    // 定义z的运算 &&
    let z = x && y;
    println!("The value of is {}",z);
    // 定义w的运算
    let w = x || y;
    print!("The value of is {}",w);
    // 定义 m的运算
    let m =x ^ y;
    print!("The value of is {}",m)

}